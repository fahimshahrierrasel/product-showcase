FROM node:22-alpine AS base

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

# Install dev tools (ts-node-dev for hot reload)
RUN npm install --only=dev

EXPOSE 5000

CMD ["npm", "run", "dev"]
