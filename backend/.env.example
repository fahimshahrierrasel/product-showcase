# Backend Environment Variables

# Server Port
PORT=5000

# Database Configuration
DB_HOST=localhost
DB_USER=user
DB_PASSWORD=password
DB_NAME=product_db
DB_DIALECT=mysql

# Seed Database on startup (set to "false" to disable)
SEED_DATABASE=true
