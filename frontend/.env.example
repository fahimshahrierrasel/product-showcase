# Frontend Environment Variables

# API URL for server-side calls (use Docker service name in Docker, localhost otherwise)
API_URL=http://backend:5000/api

# API URL for client-side calls (must be accessible from browser)
NEXT_PUBLIC_API_URL=http://localhost:5000/api
